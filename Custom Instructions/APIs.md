# Sample Prompts for API Projects

## Agent Operating Manual
```markdown
/init “Agent Operating Manual”
```


## API .NET
```markdown
/init Create custom Copilot instructions for this repository to build ASP.NET Core APIs (.NET 8). Include: minimal APIs vs controllers conventions, DI and Options pattern, EF Core migration rules, auth/authorization rules, ProblemDetails error handling, logging/PII rules, and required validation + test commands (dotnet format/build/test). Ask me only for missing repo-specific commands and folder layout.
```





